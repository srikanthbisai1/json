{
  "name": "json-server",
  "version": "1.0.0",
  "main": "server.js",
  "scripts": {
  "start": "json-server --host 0.0.0.0 --watch db.json --port $PORT"
  },
  "dependencies": {
    "json-server": "^0.17.3"
  }
}
